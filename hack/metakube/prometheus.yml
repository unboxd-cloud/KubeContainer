global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: kubecontainer-operator-local
    metrics_path: /metrics
    static_configs:
      - targets:
          - localhost:8080
        labels:
          fabric: metakube
          component: kubecontainer-operator
