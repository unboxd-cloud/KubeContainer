# The Primitives

Status: v1, normative. Every word below is defined in a single,
unambiguous statement that explains itself without any external
reference. The list is ordered: a definition may use plain language
and the primitives defined above it — never one below it, never a
document, never a brand, never a product. These are the kernels;
everything else this house writes is commentary on them. Where a
richer text elaborates a primitive, the primitive governs.

## The parties

- **Founder** — the single individual who finds a gap and dares to
  build what fills it; the title belongs to the finding, the daring,
  and the building themselves, not to any company, stage, or share,
  and it needs no one's approval — no party grants it, no party can
  revoke it, the building itself confers it. You find a bug and
  start fixing it on your own — that is founding, at its smallest
  and already whole. (This displaces, deliberately, what the word
  has come to mean. And the one who finds is named *founder*, not
  *finder*, on purpose: "Finder" is already worn by a file browser,
  and a primitive never shares its name — and *founder* is the
  apter word besides: the finder of gaps is the founder of what
  fills them, the find completed only by the founding.)
- **Principal** — the one who wants something and answers for
  wanting it.
- **Party** — anyone, human or machine, who can make a promise and
  be held to it.
- **Operator** — the party who works a thing and answers for the
  working.
- **Builder** — the party who makes a thing that was not there
  before.
- **Maintainer** — the party who keeps a thing true after it is
  built; the one who builds must not maintain — the hands that made
  a thing will forgive it, and keeping requires hands that won't.
  This is separation of duty, and separation of duty is not
  separation of concern: the builder and the maintainer hold the
  same concern — the thing, kept true — divided between two parties
  so that each can check the other; concerns divide inside the
  work, duties divide between the parties, and confusing the two
  produces both monoliths and monarchs.

## The statements

- **Intent** — a statement of how things should be.
- **Declaration** — intent written down, in a form that can be
  checked.
- **Action** — a change made in the world.
- **Record** — what was written about what happened, kept so it
  cannot be quietly changed, surviving whoever wrote it.
- **Verdict** — the result of checking a statement against the
  world: it held, or it did not.
- **Claim** — a statement that has no verdict yet.
- **Evidence** — a statement joined to its verdict, with who checked
  it, how, and when.
- **Truth** — what the record and the world agree on; there is one
  version of it at a time.
- **Reason** — the why of a decision, recorded beside the decision
  itself, never separated from it; it is always written down because
  everyone has a right to reason — the right to be given the why,
  and the right to think it through for themselves, and the second
  is impossible without the first. And trust self-generates when
  the reason is declared behind every decision: it is not asked
  for, campaigned for, or spent — it grows on its own wherever the
  why is never withheld.

## The bindings

- **Promise** — a statement about one's own future action.
- **Contract** — promises exchanged between parties, written down,
  with what happens on breach agreed before it happens.
- **Duty** — the part of a contract one party must do.
- **Breach** — a promise not kept; it is named, recorded, and
  answered, never absorbed.

## The work

- **Work** — action applied toward an intent.
- **Loop** — work that repeats without being re-asked: observe what
  is, compare it to the declaration, act to close the difference,
  record the act — and again, whenever the difference returns.
- **Step** — the smallest whole piece of work; a step is taken
  entirely or not at all.
- **Path** — steps declared in advance, in order, each checked, none
  skipped.
- **Gate** — a point that lets nothing pass without a check.
- **Drift** — slow, unplanned change away from a declaration,
  unnoticed until measured.
- **Repair** — work that returns a thing to its declaration, done
  only along a path.

## The things

- **Thing** — anything with a boundary and a name.
- **Tool** — a thing used to act, that does nothing on its own.
- **Skeleton** — a structure declared before it is filled, so that
  what arrives can be assembled without guessing.
- **Box** — a thing sealed together with its description and its
  history, to be opened only by the one it is for.
- **Agent** — a program that works a loop on a principal's intent,
  under contract, where the principal is not or when the principal
  is gone.
- **Kube** — a declaration, a loop that keeps it, one face that
  answers for it, a record that survives it, and a contract that
  binds it — whole, never a part of one.
- **Soul** — what the kube is to the thing it keeps: the part that
  makes it answerable, and the part that remains when the instance
  is gone.

## The places

- **Surface** — where a thing meets everything that is not the
  thing.
- **Face** — one point on a surface that answers for the thing,
  owned by exactly one party.
- **Edge** — the one place where the outside may enter; it admits
  and never ships.
- **Port** — the one place where a delivery leaves or lands; one
  port, one service, one contract.
- **World** — a place with its own laws, its own records, and its
  own borders.
- **Base** — the ground a thing stands on, kept still by the party
  contracted to keep it still.
- **Gap** — a place where work must cross and nothing stands to
  carry it.

## The flows

- **Message** — meaning carried from one party to another without
  being changed on the way.
- **Delivery** — moving a thing whole to the one it is for, through
  a port, with its history attached.
- **Context** — everything true around a piece of work: who it is
  for, what it touches, where it stands, when it holds, and in
  which world.
- **Provenance** — the recorded chain from origin to delivery: who
  made it, who checked it, who carried it, who received it.

## The measures

- **Measurement** — saying how much, in units both parties accept
  before counting begins.
- **Unit** — the agreed amount that counts as one; everything is
  counted in whole units, never in parts.
- **Balance** — the state in which everything that left equals
  everything that arrived plus everything still held.
- **Resolution** — a difference driven to zero and recorded as
  closed.

## The grounds

- **Registry** — the record of names and what each belongs to: one
  entry per fact, nothing removed, nothing duplicated.
- **Fabric** — all the kubes and the space between them, where
  empty space is filled only by kubes.
- **Platform** — what others stand on to work: it holds the paths,
  the records, the registry, and its own contract — and answers
  for all four.

## The rule of this list

A word on this list means exactly this and nothing else, everywhere
this house writes. A new primitive is added only when work cannot be
said without it; a primitive is changed only by recorded amendment;
and any sentence in any document that cannot be rebuilt from these
words and plain language is decoration, not doctrine.
